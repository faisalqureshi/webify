<!DOCTYPE html>
<html lang="en">
  <head>
  </head>
  <body>
    <div class="container">
        <h1>{{ __blog_title__ }}</h1>
        
        <h2>Posts</h2>

        <ul>
            {% for post in __blog_posts__ %}
                <li><a href="{{ post.link }}">{{ post.title }}</a></li>
            {% endfor %}
        </ul>

    </div>
  </body>
</html>